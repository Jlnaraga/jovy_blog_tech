<div class="container">
    <div class="row">
        <h2 class="text-muted">Welcome to Tech Blog</h2>
        <hr>
    </div>
    <div class="row">

        {{#each posts as |post|}}
            {{> blog post=post}}
        {{/each}}
        
    </div>
  </div>
</div>